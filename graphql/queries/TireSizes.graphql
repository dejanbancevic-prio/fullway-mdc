query TireSizes($input: TireSizeAttributes) {
  getTireSize(input: $input) {
    aspect_ratios
    rim_diameters
    section_widths
    tire_size_info {
      hasFrontOptions
      hasRearOptions
      size_label
      categoryUrl
    }
  }
}
